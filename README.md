# Divar Price Analyzer Bot 🤖

یک ربات تلگرام که میانگین قیمت کالاها و خودروها را از سایت **دیوار تهران** استخراج و به کاربران نمایش می‌دهد. این پروژه با **Python** و **Selenium** برای اسکرپینگ و **python-telegram-bot** برای مدیریت ربات ساخته شده است.

---

## 🧩 ویژگی‌ها

* دریافت نام محصول یا خودرو از کاربر.
* جستجوی ۲۰ آگهی اخیر در دیوار تهران.
* استخراج قیمت‌ها و محاسبه **میانگین قیمت**.
* پاسخ به کاربر با قالب‌بندی خوانا و ساده.
* مدیریت خطاها و اطلاع‌رسانی مناسب به کاربر:

  * جستجوی کوتاه یا نامفهوم
  * عدم یافتن آگهی
  * آگهی‌های بدون قیمت
  * خطاهای سیستم

---

## ⚙️ نحوه کار پروژه

### ۱. بخش اسکرپر (`DivarAnalyzer`)

* با **Selenium** مرورگر کروم را در حالت **Headless** باز می‌کند (بدون نمایش پنجره مرورگر).
* آگهی‌ها را از صفحه‌ی جستجو دریافت می‌کند.
* قیمت‌ها را از متن استخراج و اعداد فارسی را به انگلیسی تبدیل می‌کند.
* میانگین قیمت را محاسبه و برمی‌گرداند.

### ۲. بخش ربات تلگرام (`DivarBot`)

* با **توکن ربات** راه‌اندازی می‌شود.
* به دستورات `/start` پاسخ می‌دهد و راهنمایی کاربر را نمایش می‌دهد.
* پیام‌های متنی کاربر را پردازش کرده و نتیجه تحلیل قیمت را برمی‌گرداند.
* مدیریت خطاها و اطلاع‌رسانی مناسب به کاربر انجام می‌دهد.

---

## 🛠 پیش‌نیازها

* Python 3.10 یا بالاتر
* کتابخانه‌ها:

```bash
pip install selenium webdriver-manager python-telegram-bot
```

* مرورگر Google Chrome نصب شده روی سیستم
* توکن ربات تلگرام از [@BotFather](https://t.me/BotFather)

---

## 🚀 اجرای پروژه

۱. توکن ربات خود را از @BotFather دریافت کنید و در کد جایگزین کنید:

```python
MY_TOKEN = 'توکن_ربات_شما'
```

۲. برنامه را اجرا کنید:

```bash
python main.py
```

۳. در تلگرام، ربات خود را باز کرده و دستور `/start` را ارسال کنید.

۴. نام محصول یا خودرو را وارد کنید، ربات میانگین قیمت آگهی‌های دیوار را نمایش می‌دهد.

---

## 🔧 مدیریت خطاها

* **عبارت جستجو خیلی کوتاه:** ⚠️ لطفاً جزئیات بیشتری وارد کنید.
* **نتیجه‌ای پیدا نشد:** ❌ املای کلمات را بررسی کنید.
* **آگهی‌ها بدون قیمت:** 💡 قیمت مشخصی ندارند (توافقی هستند).
* **خطا در ارتباط با دیوار:** 🛠 لحظاتی دیگر تلاش کنید.

---

## 📚 نکات فنی

* محدود به بررسی **۲۰ آگهی اخیر** برای هر جستجو.
* تنها آگهی‌هایی با قیمت مشخص (تومان) پردازش می‌شوند.
* اعداد فارسی به انگلیسی تبدیل می‌شوند تا محاسبات صحیح انجام شود.
* مرورگر کروم در حالت **Headless** اجرا می‌شود تا منابع کمتری مصرف شود.

---

## 💡 پیشنهادات برای توسعه

* افزودن قابلیت بررسی شهرهای دیگر غیر از تهران.
* ذخیره‌ی نتایج در دیتابیس برای تحلیل‌های تاریخی.
* نمایش بیشترین و کمترین قیمت علاوه بر میانگین.
* افزودن دکمه‌های تلگرام برای جستجوی سریع‌تر.

---

## 📝 نتیجه

این ربات برای کسانی که می‌خواهند **میانگین قیمت کالاها و خودروها در دیوار تهران** را بدانند، مفید است و با رابط کاربری ساده تلگرام، تجربه‌ی کاربری روان و سریع ارائه می‌دهد.
